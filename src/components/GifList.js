import React, {Component} from 'react';

class GifList extends Component {


}
export default GifList = (props) => {
    const list = props.gifs.map((gif, i) => {
      return(  <li><img >{this.list}</img> </li>
      
        )
        }
    )
    }